<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>update</title>
</head>
<body>
    <form action="/doUpdate" method="get">
        <p>姓名：<input type="text" name="name" value="<%=student.name%>"></p>
        <p>年龄：<input type="text" name="age" value="<%=student.age%>"></p>
        <p>性别：<input type="radio" name="gender" value="男" <%if(student.gender=='男'){%>checked="checked"<%}%>>男<input type="radio" name="gender" value="女" <%if(student.gender=='女'){%>checked="checked"<%}%>>女</p>
        <p>
            选修课：
            <%for(var i=0;i<allCourse.length;i++){
                if(student.course.indexOf(allCourse[i].id)!=-1){
                    var sss="checked";
                }else{
                    var sss="";
                }

            %>
            <input type="checkbox" name="course" value="<%=allCourse[i].id%>" <%=sss%>><%=allCourse[i].name%>
            <%}%>
        </p>
        <input type="hidden" name="_id" value="<%=student._id%>">
        <p><input type="submit" value="提交"></p>
    </form>
</body>
</html>